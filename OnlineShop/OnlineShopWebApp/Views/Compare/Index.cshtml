@model List<ProductViewModel>

@{
    ViewData["Title"] = ViewLocalizer["page.title"];
}

<div class="container">
    <div class="row">
        <div class="col-12 mb-4">
            <h2>@ViewLocalizer["page.header"]</h2>
        </div>
        @foreach (var product in Model)
        {
            <div class="col-4 mb-4">
                <img class="img-thumbnail mb-2" src="@product.ImagePath" alt="@product.Name">
                <a style="color: darkblue"
               asp-controller="Product" asp-action="Index" asp-route-id="@product.Id"><h5>@product.Name</h5></a>
                <h6>$@product.Cost</h6>
                <p>@product.Description</p>
                <div class="row justify-content-between">
                    <div class="col-auto text-left">
                        <a class="btn btn-outline-dark btn-sm text-nowrap" role="button"
                       asp-area="" asp-controller="Basket" asp-action="Add" asp-route-id="@product.Id">@ViewLocalizer["button.add"]</a>
                    </div>
                    <div class="col-auto text-right">
                        <a class="btn btn-outline-dark btn-sm text-nowrap" role="button"
                       asp-controller="Compare" asp-action="Remove" asp-route-id="@product.Id">@ViewLocalizer["button.remove"]</a>
                    </div>
                </div>
            </div>
        }
    </div>
    <div class="row mt-4">
        <div class="col-6 text-right">
            <a class="btn btn-outline-dark btn-sm text-nowrap" role="button" href="/">@ViewLocalizer["button.continue"]</a>
        </div>
        <div class="col-6 text-left">
            <a class="btn btn-outline-dark btn-sm text-nowrap" role="button"
               asp-controller="Compare" asp-action="Clear">@ViewLocalizer["button.clear"]</a>
        </div>
    </div>
</div>





